# Kinvia frontend

React + Vite single-page app for Kinvia. See the [project root README](../README.md) for the full
setup instructions, architecture, and API reference.

Quick start:

```bash
cp .env.example .env   # point VITE_API_BASE_URL at your backend if not http://localhost:8080
npm install
npm run dev
```
